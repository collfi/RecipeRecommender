This is project at FI MUNI to PV254 Recommender systems. This is webapp aplication with recommender engine.

You should run makefile to install all the required libraries(mongokit, mongodb, flask,...). You should have also mongodb installed on your machine.
Run: python flaskr.py // to run the webapp
Edit your crontab.txt file, or run the engine.py manually.